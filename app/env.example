# Copy this file to .env.local

# ── API (required) ────────────────────────────────────────────────────────────
VITE_API_URL=http://localhost:3001

# ── Auth (optional in dev — app runs in demo mode without it) ─────────────────
# VITE_CLERK_PUBLISHABLE_KEY=pk_test_...

# ── Firebase (no longer used in local dev) ────────────────────────────────────
# Remove or leave blank — firebase.js is now a no-op stub
