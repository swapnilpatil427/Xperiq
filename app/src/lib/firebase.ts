// Firebase removed — all data now goes through the Express API at VITE_API_URL.
// This stub keeps old import paths working without errors during the transition.

export const isConfigured = (): boolean => false;
export const getApp = (): null => null;
export const getDb  = (): null => null;
