# ── Runtime ────────────────────────────────────────────────────────────────────
# If pip warns that `langchain` 0.0.x conflicts with pydantic / langsmith, you
# still have the obsolete monolithic package installed. LangGraph only needs
# `langchain-core`. Remove the old wheel, then reinstall:
#   pip uninstall -y langchain && pip install -r requirements.txt
fastapi[standard]>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7

# Anthropic SDK (prod Claude models — tool use for reliable schema output)
anthropic>=0.49

# LangGraph stack — explicit pins keep the resolver on pydantic v2–compatible wheels
langchain-core>=0.3.45
langsmith>=0.1.0
langgraph>=0.2
langgraph-checkpoint-postgres>=2.0

# Postgres async driver (shared by LangGraph checkpointer and db.py pool)
# (psycopg 3.x has no `async` extra — async API is built in; use `binary` wheels)
psycopg[binary]>=3.1
psycopg-pool>=3.2

# Observability
prometheus-client>=0.20
structlog>=24.0

# Config
python-dotenv>=1.0

# Redis Streams consumer (insight_events pipeline)
redis[asyncio]>=5.0

# ── Dev / test (install with: pip install -r requirements.txt -r requirements-dev.txt) ──
# See requirements-dev.txt
