{
  "private": true,
  "name": "experient",
  "scripts": {
    "start":       "npm run infra && npm run dev",
    "dev":         "concurrently --kill-others-on-fail -n \"api,web\" -c \"cyan,magenta\" \"npm --prefix backend run dev\" \"npm --prefix app run dev\"",
    "infra":       "docker compose up -d",
    "infra:stop":  "docker compose down",
    "infra:reset": "docker compose down -v && docker compose build postgres && docker compose up -d",
    "infra:logs":  "docker compose logs -f",
    "install:all": "npm install && npm --prefix backend install && npm --prefix app install"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
